function NOT(n) {
  return !n ? 1 : 0;
}

function NOT(n) {
  return n === 0 ? 1 : 0;
}

function AND(a, b) {
  return a === 1 && b === 1 ? 1 : 0;
}

function OR(a, b) {
  return a === 1 || b === 1 ? 1 : 0;
}

function toInt(str) {
  return parseInt(str);
}

function toStr(int) {
  return `${int}`;
}

function toInt(str) {
  return parseInt(str);
}

function toStr(int) {
  return int.toString();
}

const toInt = (s) => +s;
const toStr = (x) => x + '';

function toInt(str) {
  return Number(str);
}

function toStr(int) {
  return String(int);
}
